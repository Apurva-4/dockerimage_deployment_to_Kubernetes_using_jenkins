apiVersion: v1
kind: Service
metadata:
  name: httpd-service
spec:
  selector:
    app: apache 
  type: NodePort
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 3000
    nodePort: 31110

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: httpd-deployment
  labels:
    app: apache
spec:
  replicas: 1
  selector:
    matchLabels:
      app: apache
  template:
    metadata:
      labels:
        app: apache 
    spec:
      containers:
      - name: httpd-app
        image: 2002456/httpd:latest
        ports:
        - containerPort: 3000
      